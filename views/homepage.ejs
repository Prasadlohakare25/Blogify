<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Homepage</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>
    <div><%- include('./components/navbar') %></div>
    <!-- <h1 class="font-serif text-center text-red-500 text-4xl">
        hello from <%= JSON.stringify(user) %>
    </h1> -->
    <div class="mt-3  grid grid-cols-1 md:grid-cols-4 ">
        <% blogs.forEach(blog=> { %>
            <div class=" rounded material-card mx-2 bg-white h-fit md:h-fit border border-gray-800 shadow-lg p-4">
                <div class="shadow-slate-800 w-full h-fit shadow-lg">
                    <img class="w-full h-[260px] sm:[300px] object-cover rounded-t  " src="<%= blog.coverImageUrl %>"
                        height="">
                    <div class="px-6 py-4 flex flex-col ">
                        <div class="font-bold text-xl tracking-wide uppercase mb-2">
                            <%= blog.title %>
                        </div>
                        <a href="/blog/<%= blog._id %>" class="bg-red-600 mt-2 px-6 py-2 rounded-md text-white font-semibold text-center">View Blog</a>
                    </div>
                </div>
            </div>
            <% }) %>
    </div>
</body>

</htm